'use strict';

let stacks = require('./stacks-and-queues.js');


describe('Tests to verify push to stack works', () => {
  it('should push nodes to the stack', () => {
    stack.push(1);
    stack.push(2);
    stack.push(3);
    stack.push(4);

    expect()
  })
})